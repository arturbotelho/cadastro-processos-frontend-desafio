import { MaskNpuInputDirective } from './mask-npu-input.directive';

describe('MaskNpuInputDirective', () => {
  it('should create an instance', () => {
    const directive = new MaskNpuInputDirective();
    expect(directive).toBeTruthy();
  });
});
