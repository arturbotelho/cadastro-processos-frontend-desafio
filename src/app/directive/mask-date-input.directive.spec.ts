import { MaskDateInputDirective } from './mask-date-input.directive';

describe('MaskDateInputDirective', () => {
  it('should create an instance', () => {
    const directive = new MaskDateInputDirective();
    expect(directive).toBeTruthy();
  });
});
